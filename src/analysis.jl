#Create a sparse matrix representing the ModelGraph structure.
# function getblockmatrix(graph::ModelGraph)
#     hypergraph = gethypergraph(graph)
#     linkvariables = getlinkvariables(graph)
#     #get map of link variables
#     A = sparse(hypergraph)
#     A_block = []
# end
